package org.wishfoundation.userservice.entity;

import java.io.Serializable;
/**
 * This interface represents a base item in the application.
 * It is used as a common contract for other entities that extend this interface.
 *
 * @author Sandeep Kumar
 */
public interface BaseItem extends Serializable {
    // No methods or properties are defined in this interface.
    // It serves as a marker interface for other entities.
}
