apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{$.Release.Name}}-rbac
subjects:
  - kind: ServiceAccount
    name: default
    namespace: default
roleRef:
  kind: ClusterRole
  name: {{$.Release.Name}}-service-discovery-client
  apiGroup: rbac.authorization.k8s.io  